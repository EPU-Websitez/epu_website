export const IMG_TYPE = "lg";

// API URL with fallback to prevent undefined in URLs
export const API_URL = process.env.NEXT_PUBLIC_API_URL || "https://api.epu.edu.iq";

// Base URL with fallback
export const BASE_URL = process.env.NEXT_PUBLIC_BASE_URL || "https://epu.edu.iq/";
